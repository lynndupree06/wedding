$(function () {
  // $.ajax({
  //  url: 'http://trycapsule.com/c/198568-jessica-frederick-wedding/photos'
  // }).done(function(data) { // data variable contains fetched html
  //  var parsedDom = data;
  //
  //  var images = data.match(/(https?:\/\/)?([\da-z\.-]+)\.([a-z\.]{2,6})([\/\w \.-]*)*\/?$(jpeg|jpg)/g);
  //
  //  // check if it works
  //  console.log( '[TEST] item price: ' );
  //  console.log( images );
  // });
});
