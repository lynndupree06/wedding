<!DOCTYPE HTML>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
</head>

<body>

<div>
  <strong><%= @party.name %> <%= @party.rsvp ? 'is Attending!' : 'is not Attending' %></strong><br><br>

  <p>Email: <%= @party.email %></p>

  <strong>Party Size:</strong> <%= @party.size %>
  <br><br>

  <strong>Meal Options:</strong>

  <ul>
    <% idx = 1 %>
    <% @party.guests.each do |g| %>
        <li><%= "#{g.first_name} #{g.last_name}" %>: <%= g.meal_option %></li>
        <% idx = idx + 1 %>
    <% end %>
  </ul>

  <div>
    <% if @party.rsvp_dinner.present? %>
        <strong>Attending Rehearsal Dinner:</strong> <%= @party.rsvp_dinner ? 'Yes' : 'No' %>
        <br>
        <strong>Party Size:</strong> <%= @party.size_rehearsal %>
    <% end %>
  </div>
  <br>

  <div>
    <% if @party.rsvp_brunch.present? %>
        <strong>Attending Sunday Brunch:</strong> <%= @party.rsvp_brunch ? 'Yes' : 'No' %>
        <br>
        <strong>Party Size:</strong> <%= @party.size_brunch %>
    <% end %>
  </div>
</div>

</body>
