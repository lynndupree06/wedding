<div class="tab-pane" id="shower">
  <h1>Wedding Shower Photos</h1>
  <div class="photos">
    <% idx = 0 %>
    <% @photos[:shower].each do |p| %>
        <%= image_tag p.avatar.url(:medium), :class => 'lazy', :id => "shower-#{idx}" %>
        <% idx = idx + 1 %>
    <% end %>
  </div>
</div>