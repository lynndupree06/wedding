<div class="info-box">
  <header>Our Story</header>

  <section class="our-story">
    <div class="vertical-line"></div>

    <span class="heart-divider">&hearts;</span>
    <%= render 'fred' %>

    <span class="heart-divider">&hearts;</span>
    <%= render 'jessica' %>

    <span class="heart-divider">&hearts;</span>
    <%= render 'meeting' %>

    <span class="heart-divider">&hearts;</span>
    <%= render 'engagement' %>

  </section>

  <%= render 'footer' %>
</div>
