<%= render 'common/guest_summary' %>

<h1>Listing parties</h1>

<%= link_to 'New Party', new_party_path %>

<table class="table table-hover tablesorter">
  <thead>
  <tr>
    <th>Name</th>
    <th>Address</th>
    <th>Email(s)</th>
    <th>Outer envelop</th>
    <th>Inner envelop</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @parties.each do |party| %>
      <tr>
        <td><span class="badge"><%= party.guests.size %></span> <%= party.name %></td>
        <td>
          <%= party.address %>
          <br>
          <%= party.city %>, <%= party.state %> <%= party.postal_code %>
          <br>
          <%= party.country %>
        </td>
        <td><%= party.email %></td>
        <td><%= party.outer_envelop %></td>
        <td><%= party.inner_envelop %></td>
        <td><%= link_to 'Show', party %></td>
        <td><%= link_to 'Edit', edit_party_path(party) %></td>
        <td><%= link_to 'Destroy', party, method: :delete, data: {confirm: 'Are you sure?'} %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<script>
    $(document).ready(function () {
                $("#myTable").tablesorter();
            }
    );
</script>
