<%= link_to 'New Photo', new_photo_path %>

<br><br>

<div>
  <% @photos.each do |p| %>
      <a href="<%= photo_path(p) %>">
        <%= image_tag p.avatar.url(:thumb) %>
      </a>
  <% end %>
</div>
